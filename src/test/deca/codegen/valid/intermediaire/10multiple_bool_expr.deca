{
    int x = 1, y = 2, z = 3;

    if (x != y && y <= z && z >= x) {
        println(x, " != ", y, " && ", y, " <= ", z, " && ", z, " >= ", x);
    }

    if (x < y && y <= z && z > x && z == z) {
        println(x, " < ", y, " && ", y, " <= ", z, " && ", z, " > ", x, " && ", z, " == ", z);
    }

    if ((x < y && y <= z) && (z > x && z == z)) {
        println("(", x, " < ", y, " && ", y, " <= ", z, ") && (", z, " > ", x, " && ", z, " == ", z, ")");
    }

    if ((x != y && y <= z) && z >= x) {
        println("(", x, " != ", y, " && ", y, " <= ", z, ") && ", z, " >= ", x);
    }

    if (x != y && (y <= z && z >= x)) {
        println(x, " != ", y, " && (", y, " <= ", z, " && ", z, " >= ", x, ")");
    }

    if (x == y || y > z || z >= x) {
        println(x, " == ", y, " || ", y, " > ", z, " || ", z, " >= ", x);
    }

    if (x > y || y <= z || z < x || z != z) {
        println(x, " > ", y, " || ", y, " <= ", z, " || ", z, " < ", x, " || ", z, " != ", z);
    }

    if ((x == y || y > z) || z >= x) {
        println("(", x, " == ", y, " || ", y, " > ", z, ") || ", z, " >= ", x);
    }

    if (x == y || (y > z || z >= x)) {
        println(x, " == ", y, " || (", y, " > ", z, " || ", z, " >= ", x, ")");
    }

    if ((x > y || y <= z) || (z < x || z != z)) {
        println("(", x, " > ", y, " || ", y, " <= ", z, ") || (", z, " < ", x, " || ", z, " != ", z, ")");
    }
}