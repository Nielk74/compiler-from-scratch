{
    int x = 1;
    int j = 2;
    
    if (x != j || x > x){
        println(x, " != ", j, " || ", x, " > ", x);
    }

    if (x >= 5 || x < j){
        println(x, " >= ", 5, " || ", x, " < ", j);
    }

    if (!(x > j || x == 40)){
        println("!(", x, " > ", j, " || ", x, " == ", 40, ")");
    }

    if (!!(x > j || 0 <= j)){
        println("!!(", x, " > ", j, " || ", 0, " <= ", j, ")");
    }
}