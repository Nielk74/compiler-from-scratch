{
    int x = 1;
    int j = 2;
    
    if (x != j && x >= x){
        println(x, " != ", j, " && ", x, " >= ", x);
    }

    if (!(x > j && x == 1)){
        println("!(", x, " > ", j, " && ", x, " == ", 1, ")");
    }

    if (!!(x < j && 0 <= j)){
        println("!!(", x, " < ", j, " && ", 0, " <= ", j, ")");
    }
}