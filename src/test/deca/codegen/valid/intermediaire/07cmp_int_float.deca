{
    int x = 2;
    float y = 3.5;
    if (x < 3.5) {
        println(x, " < 3.5");
    }
    
    if (x < y) {
        println(x, " < ", y);
    }

    if (y != x) {
        println(y, " != ", x);
    }

    if (2.0 >= x) {
        println("2.0 >= ", x);
    }

    if (x <= 2.0) {
        println(x, " <= 2.0");
    }

    if (!(x > y || x > 2.1)){
        println(x, " < ", y, " && ", x, " < ", 2.1);
    }

    if (!(x > y && x < 2.1)){
        println(x, " > ", y, " || ", x, " < ", 2.1);
    }

    if (!(x < y && x > 2.1)){
        println(x, " < ", y, " || ", x, " > ", 2.1);
    }
}